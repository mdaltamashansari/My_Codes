#include<stdio.h>
//Area of a rectangle using hardcoded inputs
int main(){
    int length = 4, breadth =7;
    int area = (length * breadth);
    printf("The area of a rectangle is %d",area);
    return 0;
}