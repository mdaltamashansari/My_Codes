#include <stdio.h>
int main()
{
    //An array is the collection of similar datatype stored in continuous memory location

    //How to declare an array
    //int marks[100];
    //float salary[50];
    //char grade[70];

    /**********************************************/

    //Declaring an assigning value in an array

    // int marks[4];
    // marks[0]=55;
    // marks[1]=45;
    // marks[2]=75;
    // marks[3]=84;
    // printf("%d",marks[2]);

    /**********************************************/

    //Taking user input in an array
    // int age[4];
    // printf("Enter the first age: ");
    // scanf("%d",&age[0]);
    // printf("The value of age 0 is %d",age[0]);
    // and so on you can input and output value of multiple array elements

    return 0;
}