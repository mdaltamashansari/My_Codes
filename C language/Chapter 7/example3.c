//Another way of assigning value in arrays
#include <stdio.h>
int main()
{
    // int age[5] = {5,8,12,15,18};
    // printf("The value are %d\n",age[0]);
    // printf("The value are %d\n",age[1]);
    // printf("The value are %d\n",age[2]);
    // printf("The value are %d\n",age[3]);
    // printf("The value are %d\n",age[4]);

    /**********************************************/

    //dynamically allocating the size of array with respect to elements in it.

    int arr[]= {85,64,2,45};
    int size = 
    printf("The value are %d\n",arr[0]);
    printf("The value are %d\n",arr[1]);
    printf("The value are %d\n",arr[2]);
    printf("The value are %d\n",arr[3]);

     /**********************************************/
     
    return 0;
}