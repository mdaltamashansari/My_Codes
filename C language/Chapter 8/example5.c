#include <stdio.h>
#include <string.h>
int main()
{
    //strlen function returns us with the size of the string
    // char address[] = "Pather ki Masjid, Ashok Raj Path";
    // int size = strlen(address);
    // printf("The size of string is %d",size);

    
    //strcpy is used to copy the contents of one string to another string
    // char source[] = "Harmandeep Singh";
    // char target[35];
    // strcpy(target,source);
    // puts(target);
    
    //strcat is used to add two strings
    // char target[] = "Harmandeep ";
    // char source[] = "Singh";
    //the first parameter variable in the function is where the string will get added and the second variable's content is gonna get added to the first string
    // strcat(target,source);
    // puts(target);
    // puts(source);

    //strcmp compares the value of two strings

    // int result = strcmp("name","name");
    // printf("%d",result);



    return 0;
}